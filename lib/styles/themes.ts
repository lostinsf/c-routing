import { Theme } from '@emotion/react';

export const zIndexes = {
  base: 1,
  home: 2,
  header: 100,
};

export const defaultTheme: Theme = {
  colors: {
    black: '#000',
    white: '#fff',
  },
};
